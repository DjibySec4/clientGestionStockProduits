//Entité User
//On a pas user le mdp comm attr ds le front(cote client ) pr des raisons de secutité
export class User
{
    constructor(id ?:number, username ?: string, enable ?: boolean)
    {

    }
}

